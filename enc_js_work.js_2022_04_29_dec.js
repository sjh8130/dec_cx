// https://mooc***.chaoxing.com/ananas/space/exam/js/enc_js_work.js
// Fri, 29 Apr 2022 12:xx:xx GMT

setInterval(F7(), 4000);
!function () {
	function F8() {
		var ah = true;
		return function (ai, aj) {
			var F9 = ah ? function () {
				if (aj) {
					var ak = aj.apply(ai, arguments);
					aj = null;
					return ak;
				}
			}
				: function () { };
			ah = false;
			return F9
		}
	}
	var F10 = F8(this, function () {
		function F11() { return 'dev'; }
		function F12() { return 'window'; }
		function F13() {
			var sl = new RegExp(`\\w+ *\\(\\) *{\\w+ *['|"].+['|"];? *}`);
			return !sl.test(F11.toString());
		}
		function F14() {
			var am = new RegExp('(\\\\[x|u](\\w){2,4})+');
			return am.test(F12.toString());
		}
		function F15(an) {
			// var ao = ~-1 >> 1 + 255 % 0;
			var ao = 0;
			if (an.indexOf('i' === ao)) {
				F16(an);
			}
		}
		function F16(ap) {
			//var aq = ~-4 >> 1 + 255 % 0;
			var aq = 3;
			if (ap.indexOf((true + '')[3]) !== aq) {
				F15(ap);
			}
		}
		if (!F13()) {
			if (!F14()) {
				F15('indеxOf');
			} else {
				F15('indexOf');
			}
		} else {
			F15('indеxOf');
		}
	});
	F10();
	function F17() {
		var ar = true;
		return function (as, at) {
			if ('FtzPD' === 'FtzPD') {
				var F18 = ar ? function () {
					if (at) {
						if ('diLjM' === 'kZnnY') {
							var au = at.apply(as, arguments);
							at = null;
							return au;
						} else {
							var av = at.apply(as, arguments);
							at = null;
							return av;
						}
					}
				}
					: function () { };
				ar = false;
				return F18;
			} else {
				return debuggerProtection;
			}
		};
	}
	F17(this, function () {
		var aw = new RegExp('function *\( *\)');
		var ax = new RegExp('\+\+ *(?:_0x(?:[a-f0-9]){4,6}|(?:\b|\d)[a-z0-9]{1,4}(?:\b|\d))', 'i');
		var ay = F7('init');
		if (!aw.test(ay + 'chain') || !ax.test(ay + 'input')) {
			ay('0');
		} else {
			F7();
		}
	})();
	function F19() {
		if ('FAiUx' !== 'rBSLK') {
			var az = true;
			return function (ba, bb) {
				var F20 = az ? function () {
					if ('dXHzw' !== 'dXHzw') {
						(function () { return false; }.constructor('debu' + 'gger').apply('stateObject'));
					} else {
						if (bb) {
							var bc = bb.apply(ba, arguments);
							bb = null;
							return bc;
						}
					}
				}
					: function () { };
				az = false;
				return F20;
			};
		} else {
			var bd, be = 0, bf = d(f);
			if (0 === F24.length) {
				return be;
			}
			for (bd = 0; bd < F24.length; bd++) {
				be = (be << (bf ? 5 : 16)) - be + F24.charCodeAt(bd);
				be = bf ? be : ~be;
			}
			return 2147483647 & be;
		}
	}
	var bg = F19(this, function () {
		function F21() { }
		function F22() {
			if ('MGdAY' === 'qGPnS') {
				return function (dy) { }.constructor('while (true) {}').apply('counter');
			} else {
				var bh;
				try {
					bh = Function('return (function() ' + '{}.constructor("return this")( )' + ');')();
				} catch (dz) {
					bh = window;
				}
				return bh;
			}
		}
		var bi = F22();
		if (!bi.console) {
			if ('zvtdZ' !== 'zvtdZ') {
				F7();
			} else {
				bi.console = function (dx) {
					var bj = {};
					bj.log = dx;
					bj.warn = dx;
					bj.debug = dx;
					bj.info = dx;
					bj.error = dx;
					bj.exception = dx;
					bj.trace = dx;
					return bj;
				}(F21);
			}
		} else {
			bi.console.log = F21;
			bi.console.warn = F21;
			bi.console.debug = F21;
			bi.console.info = F21;
			bi.console.error = F21;
			bi.console.exception = F21;
			bi.console.trace = F21;
		}
	});
	bg();
	function F23() {
		return Math.floor(10 * Math.random());
	}
	function F24() {
		if ('HjOiI' === 'lbMGC') {
			F7();
		} else {
			var bk,
				bl = {},
				bm = document.getElementById('userId'),
				bn = bm ? bm.value : '',
				bo = document.getElementById('workRelationId'),
				bp = bo ? bo.value : '',
				bq = '' + new Date().getTime(),
				br = F23(),
				bs = F23(),
				bt = bn + '_' + bp;
			bl.x = -1;
			bl.y = -1;
			function F25(bu) {
				return void 0 === bu.isTrusted || !(bu.isTrusted = !1) === bu.isTrusted;
			}
			MouseEvent;
			if (2 == br) {
				br = 3
			}
			try {
				var bv = window.event;
				if (void 0 === bv) {
					if ('UyZqT' !== 'UyZqT') {
						var bw;
						try {
							bw = Function('return (function() ' + '{}.constructor("return this")( )' + ');')();
						} catch (_0x512690) {
							bw = window;
						}
						return bw;
					} else {
						for (var bx = arguments.callee.caller, by = bx; null != bx;) {
							bx = (by = bx).caller;
						}
						bv = by.arguments[0];
					}
				}
				if (null != bv) {
					if ('eULRB' !== 'eULRB') {
						var bz = {};
						bz.log = func;
						bz.warn = func;
						bz.debug = func;
						bz.info = func;
						bz.error = func;
						bz.exception = func;
						bz.trace = func;
						return bz;
					} else {
						var ca = document.documentElement.scrollLeft || document.body.scrollLeft,
							cb = document.documentElement.scrollTop || document.body.scrollTop;
						bl.x = bv.pageX || bv.clientX + ca;
						bl.y = bv.pageY || bv.clientY + cb;
						true !== bv.isTrusted && void 0 !== bv.isTrusted || (br = 2);
						bk = F25(bv) ? 2 != br ? function (cc) {
							if ('LVoCw' === 'LVoCw') {
								for (var cd = 0, ce = 0, cf = F25(bv), cg = 0, ch = cc.length; cg < ch; cg++) {
									if (0 != (ce = 1879048192 & (cd = (cd << 4) + cc.charCodeAt(cg)))) {
										cd ^= ce >> (cf ? 24 : 16);
										cd &= ~ce;
									}
								}
								return 2147483647 & cd;
							} else {
								var F27 = firstCall ? function () {
									if (fn) {
										var ci = fn.apply(context, arguments);
										fn = null;
										return ci;
									}
								} : function () { };
								firstCall = false;
								return F27;
							}
						}
							: function (cj) {
								var ck, cl = 0, cm = F25(bv);
								if (0 === cj.length)
									return cl;
								for (ck = 0; ck < cj.length; ck++)
									cl = (cl << (cm ? 5 : 16)) - cl + cj.charCodeAt(ck);
								cl = cm ? cl : ~cl;
								return 2147483647 & cl;
							}
							: function (cn) {
								var co = 0;
								if (0 == cn.length) {
									return co;
								}
								for (var cp = 0; cp < cn.length; cp++) {
									co = (co << 5) - co + cn.charCodeAt(cp);
									co &= 2147483647;
								}
								return co;
							};
					}
				}
			} catch (_0x42bbec) {
				bl = {};
				bl.x = -2;
				bl.y = -2;
			}
			br = '' + br + bs + bk(ea.join('') + bq.substring(4) + br + bs + bp) % 10;
			var cq = '(' + Math.ceil(bl.x) + '|' + Math.ceil(bl.y) + ')';
			return function (cr, cs) {
				if ('AjqQF' !== 'AjqQF') {
					if (fn) {
						var ct = fn.apply(context, arguments);
						fn = null;
						return ct;
					}
				} else {
					if (null == cs || cs.length <= 0)
						return null;
					for (var cu = '', cv = 0; cv < cs.length; cv++) {
						cu += cs.charCodeAt(cv).toString();
					}
					var cw = Math.floor(cu.length / 5),
						cx = parseInt(cu.charAt(cw) + cu.charAt(2 * cw) + cu.charAt(3 * cw) + cu.charAt(4 * cw)),
						cy = Math.ceil(cs.length / 2),
						// cz = Math.pow(2, 31) - 1;
						cz = 2147483647
					if (cx < 2) {
						return null;
					}
					var da = Math.random(),
						db = Math.round(1000000000 * da) % 100000000;
					if (10 < (cu += db).length) {
						cu = parseInt(cu.substring(0, 10)).toString()
					}
					cu = (cx * cu + cy) % cz;
					var dc = '',
						dd = '';
					for (cv = 0; cv < cr.length; cv++) {
						dd += (dc = parseInt(cr.charCodeAt(cv) ^ Math.floor(cu / cz * 255))) < 16 ? '0' + dc.toString(16) : dc.toString(0x10);
						cu = (cx * cu + cy) % cz;
					}
					for (db = db.toString(16); db.length < 8;) {
						db = '0' + db;
					}
					return (dd += db) + '&rd=' + da;
				}
			}(cq, bt = bt + '|' + br) + '&value=' + cq + '&wid=' + bp + '&_edt=' + (bq + br);
		}
	}
	var ea = ['f', '0', '4', '2', 'f', 'c', 'f', '1', 'f', '4', '8', '3', 'b', '0', '5', 'a', 'e', '5', 'f', '3', '1', '6', 'd', '0', '2', 'c', 'd', 'd', '0', '2', 'd', '6'];
	window.getEnc = function () {
		return F24();
	};
}();
function F7(de) {
	function F26(df) {
		if (typeof df === 'string') {
			return function (dg) { }
				.constructor('while (true) {}').apply('counter');
		} else {
			if ('ynkOu' !== 'OqQrG') {
				if (('' + df / df).length !== 1 || df % 20 === 0) {
					(function () {
						if ('fSkme' === 'yoglM') {
							for (var dh = 0, di = 0, dj = d(f), dk = 0, dl = t.length; dk < dl; dk++)
								if (0 != (di = 1879048192 & (dh = (dh << 4) + t.charCodeAt(dk)))) {
									dh ^= di >> (dj ? 24 : 16);
									dh &= ~di
								};
							return 2147483647 & dh;
						} else {
							return true;
						}
					}
						.constructor('debu' + 'gger').call('action'));
				} else {
					if ('nAHLA' === 'nAHLA') {
						(function () {
							return false;
						}
							.constructor('debu' + 'gger').apply('stateObject'));
					} else {
						e = {};
						e.x = -2;
						e.y = -2;
					}
				}
			} else {
				if (null == e || e.length <= 0) {
					return null;
				}
				for (var dm = '', dn = 0; dn < e.length; dn++) {
					dm += e.charCodeAt(dn).toString();
				}
				var dp = Math.floor(dm.length / 5),
					dq = parseInt(dm.charAt(dp) + dm.charAt(2 * dp) + dm.charAt(3 * dp) + dm.charAt(4 * dp)),
					dr = Math.ceil(e.length / 2),
					ds = Math.pow(2, 31) - 1;
				if (dq < 2) {
					return null;
				}
				var dt = Math.random(),
					du = Math.round(1000000000 * dt) % 100000000;
				10 < (dm += du).length && (dm = parseInt(dm.substring(0, 10)).toString());
				dm = (dq * dm + dr) % ds;
				var dv = '',
					dw = '';
				for (dn = 0; dn < t.length; dn++) {
					dw += (dv = parseInt(t.charCodeAt(dn) ^ Math.floor(dm / ds * 255))) < 16 ? '0' + dv.toString(16) : dv.toString(16);
					dm = (dq * dm + dr) % ds;
				}
				for (du = du.toString(16); du.length < 8;)
					du = '0' + du;
				return (dw += du) + '&rd=' + dt;
			}
		}
		F26(++df);
	}
	try {
		if ('mIzXw' === 'mIzXw') {
			if (de) {
				if ('bCDBg' !== 'ZZGZr') {
					return F26;
				} else {
					return false;
				}
			} else {
				F26(0);
			}
		} else {
			(function () {
				return true;
			}
				.constructor('debu' + 'gger').call('action'));
		}
	} catch (_0x237387) { }
}